TELEGRAM_BOT_TOKEN=

# Comma-separated list of Telegram User IDs allowed to use the bot.
# If empty or not set, the bot will be open to everyone.
ALLOWED_USERS=12345678,87654321

# Rotate agent thread after N turns to limit accumulated context.
# 0 = disabled (default). Recommended: 20-30 for long conversations.
# AIPAL_THREAD_ROTATION_TURNS=25

# Force thread rotation when accumulated context exceeds this many chars.
# Default: 40000 (~10K tokens). Prevents runaway cost on long threads/crons.
# AIPAL_THREAD_MAX_CONTEXT_CHARS=40000

# Max chars to store per assistant response in memory capture.
# Lower values = less tokens injected via bootstrap/retrieval. Default: 500.
# AIPAL_MEMORY_CAPTURE_MAX_CHARS=500

# Daily token budget for usage tracking and alerts.
# 0 = no limit (default), only tracks usage. Set a value to enable
# proactive Telegram alerts at 25%, 50%, 75%, 85%, 95% thresholds.
# When budget is exceeded, new messages are blocked until next day.
# AIPAL_TOKEN_BUDGET_DAILY=100000

# Skip cron jobs when token usage exceeds this % of daily budget.
# Default: 90. Only effective when AIPAL_TOKEN_BUDGET_DAILY is set.
# AIPAL_CRON_BUDGET_GATE_PCT=90
